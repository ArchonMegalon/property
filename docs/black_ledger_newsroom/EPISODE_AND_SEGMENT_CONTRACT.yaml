version: black-ledger-newsroom-episode-v1
entities:
  NewsroomEpisode:
    episode_id: string
    episode_type: turn_newsreel|breaking_breach|faction_bulletin|karma_forge_report|event_invite|weekly_city_pulse
    title: string
    public_safe_summary: string
    status: draft|editorial_ready|rendering|qa_pending|published|failed|rejected
    publish_scope: public|registered|faction|gm_private|operator_only
    source_receipt_ids: string[]
    truth_layer: receipt_backed|editorial_reconstruction|fictionalized_public_safe
    anchor_id: mara_voss|brack_kade|other
    duration_target_seconds: integer
    language: en|de|multi
    created_at_utc: datetime
    updated_at_utc: datetime
  NewsroomSegment:
    segment_id: string
    episode_id: string
    order: integer
    headline: string
    script_lines: string[]
    lower_third: string
    ticker_lines: string[]
    broll_cues: BrollCue[]
    visual_truth: literal_capture|public_safe_reconstruction|illustrative_broll|editorial_graphic
    source_receipt_ids: string[]
  BrollCue:
    cue_id: string
    duration_seconds: number
    scene_type: facility_exterior|street_camera|geoscape|faction_room|matrix_trace|ritual_site|safehouse|dockyard|dossier_graphic
    prompt: string
    public_safety_notes: string
    render_status: pending|rendered|failed|rejected
